name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-15
          - windows-latest

    steps:
      - name: Checkout GeoTools plugin
        uses: actions/checkout@v4
        with:
          path: hop-geotools-plugin

      - name: Checkout Geometry type plugin
        uses: actions/checkout@v4
        with:
          repository: edigonzales/hop-geometry-type-plugin
          path: hop-geometry-type-plugin

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Install Geometry type dependency
        working-directory: hop-geometry-type-plugin
        run: mvn -U -B -ntp clean install

      - name: Build and test GeoTools plugin
        working-directory: hop-geotools-plugin
        run: mvn -U -B -ntp clean verify

      - name: Check plugin distribution
        working-directory: hop-geotools-plugin
        run: python scripts/check-distribution.py
